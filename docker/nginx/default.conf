server {
    listen 80;
    server_name localhost _;
    root /var/www/html/current/public;
    index index.php index.html;

    client_max_body_size __EXTPLORER_UPLOAD_LIMIT__;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";

    # This endpoint must stay independent from PHP, sessions, DNS and TLS.
    location = /health {
        access_log off;
        default_type application/json;
        return 200 '{"status":"ok"}\n';
    }

    location / {
        try_files $uri $uri/ /index.php$is_args$args;
    }

    location ~ ^/(writable|app|tests|vendor|spark) {
        deny all;
        return 403;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass extplorer-app:9000;
        # Traefik terminates TLS before the request reaches Nginx. Preserve
        # its scheme marker so CodeIgniter can recognize the public request
        # as HTTPS after applying its trusted-proxy allowlist.
        fastcgi_param HTTP_X_FORWARDED_PROTO $http_x_forwarded_proto;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_index index.php;
        fastcgi_read_timeout 120s;
    }

    location ~ /\.ht {
        deny all;
    }
}
