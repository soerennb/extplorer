{
  "name": "extplorer3-e2e",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "e2e": "playwright test",
    "e2e:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
